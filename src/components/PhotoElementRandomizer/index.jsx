/**
 * PhotoElementRandomizer - Main Orchestrator
 * 
 * This is the entry point that orchestrates all sub-components
 * The actual implementation will be split into smaller components
 */

import React from 'react';
import { PhotoElementRandomizerContent } from './PhotoElementRandomizerContent';

// Re-export the main component
export default PhotoElementRandomizerContent;

